export const stringUppercase = (str) => str.toUpperCase();
export const stringLowercase = (str) => str.toLowerCase();
export const stringPadStart = (str, length, padChar) =>
  str.padStart(length, padChar);
export const stringPadEnd = (str, length, padChar) =>
  str.padEnd(length, padChar);
